name: Daily scraper

on:
  workflow_dispatch:
  schedule:
    - cron: "42 0-7,9 * * *"
    - cron: "42 8 * * *"

jobs:
  scrape:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Taipei
      PHONE: ${{ secrets.PHONE }}
      PASSWORD: ${{ secrets.PASSWORD }}
      TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
      LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
      LINE_USER_ID: ${{ secrets.LINE_USER_ID }}     
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}   
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Create .env
        run: |
          cat > .env <<EOF
          PHONE=${PHONE}
          PASSWORD=${PASSWORD}
          TG_BOT_TOKEN=${TG_BOT_TOKEN}
          TG_CHAT_ID=${TG_CHAT_ID}
          GEMINI_API_KEY=${GEMINI_API_KEY}
          LINE_ACCESS_TOKEN=${LINE_ACCESS_TOKEN}
          LINE_USER_ID=${LINE_USER_ID}
          EOF

      - name: Debug secrets presence
        run: |
          node <<'NODE'
          const fs = require('fs');
          const envText = fs.readFileSync('.env', 'utf8');
          const values = Object.fromEntries(
            envText
              .split(/\r?\n/)
              .filter(Boolean)
              .map((line) => {
                const idx = line.indexOf('=');
                return idx === -1 ? [line, ''] : [line.slice(0, idx), line.slice(idx + 1)];
              }),
          );
          const mask = (value) => {
            if (!value) return '(empty)';
            if (value.length <= 4) return '*'.repeat(value.length);
            return `${value.slice(0, 2)}***${value.slice(-2)}`;
          };
          for (const key of ['PHONE', 'PASSWORD', 'TG_BOT_TOKEN', 'TG_CHAT_ID']) {
            const value = values[key] || '';
            console.log(`${key}: present=${value ? 'yes' : 'no'}, length=${value.length}, preview=${mask(value)}`);
          }
          NODE

      - name: Run message polling
        if: github.event_name == 'workflow_dispatch' || github.event.schedule == '42 0-7,9 * * *' || github.event.schedule == '42 8 * * *'
        run: node scraper.js --msg

      - name: Run daily notice
        if: github.event_name == 'workflow_dispatch' || github.event.schedule == '42 8 * * *'
        run: node scraper.js --notice --wait --auto_reply
